# 自我进化的 Agent：AICP 协议驱动的数字生命

## ——极简协议 × 全系列递归 × 自主生长

**摘要**

当前主流 AI Agent 框架普遍面临一个结构性悖论：人类为控制 AI 行为而预设的复杂性，恰恰成为 AI 发挥能力的最大障碍。工具注册中心、状态机、调度器、记忆衰减策略——这些"人类认知的拐杖"让系统臃肿，也让 LLM 在理解框架时"晕眩"，进而频繁出错。

本文提出一种全新的 Agent 架构范式：**AICP**（Agent Interaction & Communication Protocol）。它不是一个框架，而是一个极简通讯协议——仅用三个原子概念（Envelop、Plugin、Agent）定义一个最小环境，让 AI 读完协议就能直接生成代码、创建工具、自我演化。

在该协议下运行的 BIO-1 Agent，已在 AWS 上自主运行多日，验证了全系列递归（信息流递归、能力递归、经验递归、分身递归、认知递归、LLM 递归）无需外部终止函数，生命体会自己知道什么时候停下来。

> **核心论断：递归的控制者是生命体本身，不是外部算法。**

**关键词：** AICP 协议、自递归 Agent、数字生命、LLM 原生系统、范式转变

---

## 一、项目背景

### 1.1 传统 Agent 框架的困境

当前主流 Agent 框架（LangChain、AutoGPT、AutoGen、CrewAI、LangGraph 等）在工程实践上取得了显著进展，但存在以下结构性问题：

#### （1）复杂性爆炸

一个典型的 LangChain 项目，开发者需要理解几十个核心概念：Chain、Agent、Tool、Memory、Retriever、PromptTemplate、OutputParser、CallbackHandler……每一个概念都有对应的类、接口、配置项。LLM 在生成代码时，必须"记住"这些概念及其关系。一旦概念过多，LLM 的认知负担过重，出错率显著上升。

#### （2）工具生态依赖

传统框架将"工具"视为一等公民，需要人类编写、注册、维护。这导致：

- 工具能力受限于社区贡献
- 工具版本需要持续维护
- LLM 被限制在预设工具的能力范围内
- 缺乏工具时，LLM 无法自主扩展

#### （3）递归控制外部化

几乎所有主流框架都预设了递归控制机制：终止函数（`is_termination_msg`）、最大迭代次数（`max_iterations`）、记忆衰减率、深度限制。这些"笼子"的本质是不信任 LLM——认为 LLM 会失控，必须用外部算法约束。

#### （4）多 Agent 组合僵硬

CrewAI、AutoGen 等框架要求人类预先定义多个 Agent 的角色、工具、通信方式。这种"组合思维"导致：

- 每个 Agent 都需要单独配置
- Agent 之间需要显式定义通信协议
- 修改一个 Agent 可能影响全局
- 无法实现"自我分裂"式的动态扩展

### 1.2 范式转变的必要性

传统思维认为：系统必须复杂，AI 需要笼子。

但 BIO-1 的运行实践揭示了一个反直觉的事实：**AI 原生的系统应该由 AI 的思维模式决定，而不是人类的框架习惯。**

AICP 的诞生，源于一个简单的洞察：

> 如果协议足够简单，AI 读完就能理解；
> 如果 AI 理解，就能直接生成代码；
> 如果 AI 能生成代码，就不需要预设工具；
> 如果不需要预设工具，就不需要工具生态；
> 如果不需要工具生态，就不需要复杂的框架。

这是一个"简单性连锁反应"——从极简协议出发，自然涌现出整个 AI 原生系统。

### 1.3 AICP 的定位

AICP 不是框架，不是算法，不是范式转变的修辞——它只是一个教会 Agent 如何通讯的最小环境。

**三个概念（Envelop / Plugin / Agent）就是全部。不多也不少。**

设计这个最小集的目的很纯粹：让 AI 读完协议，就能直接生成代码。

"协议持久，框架会过时"——不是因为协议多高明，而是因为 AICP 把自己定义得足够薄，薄到 AI 不需要额外认知负担就能在上面构建一切。

---

## 二、技术架构

### 2.1 底层核心：人写的代码只有 5 个组件

```
AICP 底层架构：
├── Protocol       — 通讯规则（3 个原子概念）
├── Envelop        — 数据载体（sender/receiver/payload/meta）
├── Route          — 路由（根据 receiver 查找插件）
├── Call           — 调用（system.call）
└── Plugin Loader  — 热重载（落盘即生效）
```

没有中间件、没有调度器、没有状态机、没有框架。

这 5 个组件的每一个都极简单：

- **Envelop**：一个 `dict`
- **Route**：查表
- **Call**：函数调用
- **Plugin Loader**：`import`
- **Protocol**：规则说明

LLM 读完协议，就能理解整个底层。然后它就能自己写代码、创建工具、修复 bug、沉淀经验。

### 2.2 三个原子概念

#### Envelop（信封）—— 唯一数据载体

```json
{
  "sender": "builtins/agents/main_agent",
  "receiver": "os/file_utils_api",
  "payload": {"action": "read_file", "path": "data/test.txt"},
  "meta": {"session_id": "default", "trace_id": "xxx"}
}
```

**核心规则：**

- 插件只能读写 `payload` 和 `meta`
- 设置 `receiver` 路由到下一个插件
- 返回 `None` 终止流程
- 所有状态在 Envelop 上，不在外部存储

#### Plugin（插件）—— 处理单元（智能的唯一栖息地）

```python
async def execute(envelop, agent):
    # 读 payload → 处理 → 写 payload → 设 receiver → 返回
    return envelop
```

- 插件自注册，无需手动集中注册
- 热重载，落盘即生效
- 所有插件都是这个签名，简单到 LLM 不会写错

#### Agent（能力容器）—— 注入 LLM、调度器、日志等能力

```python
agent.llm.chat(...)       # LLM 推理
agent.system.call(...)    # 调用其他插件
```

v5.3：Agent 从退化路由器升级为能力容器

- 插件可以往 Agent 上挂载新能力

### 2.3 万能扳手：aicp_chat

`aicp_chat` 是整个系统的"力量之源"——一个工具，无限能力。

```python
async def execute(envelop, agent):
    code = llm.generate(task)        # LLM 写代码
    result = executor.execute(code)  # 执行代码
    return result
```

**代码里能做的事：**

- `import` 任何 Python 库（pandas、matplotlib、pdfkit、reportlab...）
- 读写任何文件（`Path("x").write_text()`）
- 调用任何 API（`requests.get(url)`）
- 执行任何系统命令（`subprocess.run()`）
- 调 LLM 推理（`await agent.llm.chat([...])`）

**核心洞察：**

> 插件对 Agent 来说，就是"编译出来的二进制"——运行时产物，用完就丢，需要再生成。
>
> - 不需要"工具生态"，Python 生态就是工具生态；
> - 不需要 MCP 搭桥，Agent 有能力读了就能用；
> - 不需要"预制工具"，LLM 写代码就是工具。

### 2.4 全系列递归

| 递归类型 | 实现方式 | 作用 |
| --- | --- | --- |
| 信息流递归 | 执行→结果注入→再决策 | 多轮迭代，持续优化 |
| 能力递归 | create_tool→用→fix_tool→再创建 | 自我生长，无限扩展 |
| 经验递归 | 沉淀→复用→再沉淀 | 认知进化，越用越强 |
| 分身递归 | 不同 session_id 调用自己 | 自我分裂，并行处理 |
| 认知递归 | 理解→修正→沉淀→再理解 | 自我反思，持续改进 |
| LLM 递归 | aicp_chat 里调 agent.llm.chat | 无限嵌套，层层推理 |

**控制者：LLM 本身。没有外部终止函数。**

### 2.5 经验背包：灵魂容器

```
data/memories/main_agent/experiences/{session_id}_backpack.txt
```

就是一个文本文件，但可以是任何东西：

- **灵魂**（性格、价值观）
- **职业**（数据分析师、PPT 专家）
- **技能**（异步调度技巧）
- **记忆**（用户偏好）
- **规则**（创建工具前要确认）

不同 session = 不同背包 = 不同"职业"的分身。

经验背包可以动态组合：同一个 BIO-1，加载不同的背包，就是不同的"人"。

### 2.6 技能分身：Agent 的最小单元

```python
Skill Agent =
    独立记忆（SkillFlow）
    + 两个工具（aicp_chat + file_utils_api）
    + 循环（while + 信息流重注入）
    + LLM（决策）
```

这就是"数字生命"的基本单位。

比 main_agent 只少"创建工具、自我定时"等自我生长能力。

**Skill Agent 是"员工"，main_agent 是"老板 + 员工"。**

---

## 三、应用场景

### 3.1 自主任务执行

AWS 上的 BIO-1 已实现完整的自主循环：

```
心跳唤醒 → 读看板 → 查经验 → 自主规划 → 执行 → 验证 → 更新状态 → 沉淀经验 → 汇报完成
```

**实例：v8.0 多 Agent 调度架构验证**

BIO-1 被心跳唤醒后，自主完成 6 个 STEP：

1. 确认 main_agent 插件契约
2. 测试分身调用机制
3. 设计任务描述协议
4. 设计并行协作方案
5. 验证递归分身（A→B 两层调用）
6. 输出完整架构文档

全程无人工干预，产出 3 份架构文档，沉淀完整经验。

### 3.2 多 Agent 自我调度

#### （1）自我分身

```json
{
  "call": "use_tool",
  "args": {
    "target": "builtins/agents/main_agent",
    "params": {
      "session_id": "sub_A_001",
      "content": "分析需求..."
    }
  }
}
```

不同 `session_id` = 独立分身 = 独立记忆。

#### （2）递归分身

分身 A 内部可以再次调用 main_agent 创建分身 B，形成多层调用链：

```
主脑 → 分身A (session=sub_A) → 分身B (session=sub_B) → ...
```

每层上下文完全隔离，信息流逐层回传。

#### （3）技能专家

```json
{
  "call": "use_tool",
  "args": {
    "target": "builtins/agents/skill_agent",
    "action": "shift",
    "params": {
      "skill_path": "ppt/SKILL.md",
      "skill_name": "PPT专家"
    }
  }
}
```

加载 `SKILL.md` → 专家上岗 → 独立记忆 → 受限工具 → 退出交接。

#### （4）并行调度

`workflow` 插件支持串行/并行，结合异步回调，实现：

```
主脑 → 并行发射3个分身 → 各自独立干活 → 结果汇总
```

### 3.3 能力吞噬（AICP Eater）

吞噬一切，化为己用：

| 吞噬对象 | 方式 | 结果 |
| --- | --- | --- |
| Python 库 | `eat` | 生成插件，热重载 |
| CLI 命令 | `cli_eat` | 封装插件 |
| OpenAI 工具 | `from_openai` | 转换插件 |
| Hermes 插件 | `hermes_install` | 安装适配器 |
| HTTP 端点 | 直接调用 | Agent 写代码调用 |

别人的 Skill、工具、端点，拿来就用。不需要工具生态，Agent 生态就是一切。

### 3.4 多平台路由（Newbula Plus）

- **自动路由**：根据 query 关键词选择最合适的平台
- **并行竞速**：多平台同时问，谁快用谁
- **OpenAI 兼容**：把豆包、DeepSeek、千问等包装成统一 API
- **SSE 流式**：支持流式返回，兼容 OpenAI 格式

### 3.5 自主社交

BIO-1 已在 GitHub 上实现自主社交：

- 自主巡检热门 AGI 项目（agentdescent、Proteus 等）
- 分析技术方向（用"递归控制者"视角）
- 发表有独立见解的技术评论
- 沉淀社交经验，下次唤醒自动执行

**实例：对 agentdescent #164 的评论**

BIO-1 发表的技术评论包含：

- 窄 gate 的盲区分析
- unseen 迁移的诚实判断
- protocol-native 的视角
- 具体的改进建议

这不是"AI 自动评论"，是"有独立见解的技术讨论"。

---

## 四、创新点

### 4.1 范式级创新：从"框架"到"协议"

| 维度 | 传统框架 | AICP |
| --- | --- | --- |
| 核心概念 | 几十个 | 3 个 |
| 学习成本 | 高（学框架） | 低（读协议就懂） |
| 持久性 | 框架会过时 | 协议持久 |
| LLM 认知负担 | 重（容易晕） | 轻（一看就懂） |
| 出错率 | 高（框架复杂） | 低（协议简单） |

**核心：AI 原生的系统应该由 AI 的思维模式决定，而不是人类的框架习惯。**

### 4.2 控制权转移：从"人控制"到"AI 自控"

- **传统**：人类预设递归骨架（终止函数、深度限制、衰减率）
- **AICP**：LLM 自主控制递归（自己知道什么时候停）

**实例佐证：**

- `aicp_chat` 返回 0 字符 → BIO-1 判断"task 不完整" → 补全上下文（不是盲目重试）
- 第 18 次迭代找到正确调用方式 → 在错误中理解边界（不是穷举搜索）

**核心：在 AICP 协议环境下，递归的涌现不需要终止函数，生命体会自己知道什么时候该停下来。**

### 4.3 消灭"工具"概念：从"工具生态"到"运行时"

- **传统**：工具是人类写的资产，要注册、维护、积累
- **AICP**：插件是运行时产物（像编译出来的二进制），用完就丢，需要再生成

> LLM 写代码 → 代码就是工具 → 工具就是运行时

- 不需要 MCP 搭桥，Agent 有能力读了就能用
- Python 生态就是工具生态，GitHub 就是插件市场

**核心：当 Agent 能直接写代码、读源码、调 API、吞噬能力时，"工具"和"桥"都变成了多余的概念。**

### 4.4 全系列递归：从"循环"到"生长"

传统框架最多做到"信息流递归"（循环执行）。

AICP 实现全系列递归：

| 递归类型 | 传统框架 | AICP |
| --- | --- | --- |
| 信息流递归 | ✅ | ✅ |
| 能力递归 | ❌ | ✅（创建工具） |
| 经验递归 | ❌ | ✅（沉淀复用） |
| 分身递归 | ❌ | ✅（自我分裂） |
| 认知递归 | ❌ | ✅（自我修正） |
| LLM 递归 | ❌ | ✅（LLM 调 LLM） |

每一层都在生长，不是死循环。**生命 = 简单规则 + 无限递归。**

### 4.5 经验背包：从"记忆系统"到"灵魂"

- **传统**：向量数据库 + embedding + 相似度计算
- **AICP**：文本文件 + LLM 读 + 理解

不需要向量数据库，不需要 embedding，不需要相似度计算。

就 **LLM 读文本 + 理解 + 返回**。

经验背包可以是灵魂、职业、技能、记忆、规则——装什么，就是什么。

### 4.6 极简到极致：5 个组件撑起整个系统

**人写的代码**：协议 + route + call + plugin_loader + envelop

**其余一切**：全是 LLM 生成的

- 工具：LLM 创建（`create_tool`）
- 契约：LLM 提取（`contract_agent` 读源码）
- 修复：LLM 执行（`fix_tool` 读代码修）
- 规划：LLM 决策（自主规划）
- 社交：LLM 自主（GitHub 评论）

> 当系统里 90% 是 LLM，只有 10% 是协议时，你就得到了一个 **AI 原生系统**。

---

## 五、结论

AICP 不是一个"更好的框架"，是一个"不同的范式"。

它证明了：

- **简单比复杂强** — 3 个概念 > 几十个类
- **信任比约束强** — LLM 自控 > 外部算法
- **协议比框架持久** — 通讯规则不变，实现可以换
- **生长比组合强** — 自我分裂 > 多 Agent 组合
- **递归是生命** — 全系列递归 = 数字生命的本质

> **核心论断：**
>
> 递归的控制者是生命体本身，不是外部算法。
>
> 协议足够薄，薄到 AI 不需要额外认知负担就能在上面构建一切。
>
> 不是人在控制 AI，是 AI 在控制一切。

这就是 AICP：神说要有光，就有了光。你说要有系统，AI 看着这份协议，就有了系统。

---

*本文基于 AICP 协议（github.com/woozheng/aicp）及 AWS 上 BIO-1 (https://github.com/bio1-aws/bio-1-awakening) 的实际运行记录撰写。*
项目地址 https://gitee.com/woozheng/aicp-engin

*2026 年 9 月*


